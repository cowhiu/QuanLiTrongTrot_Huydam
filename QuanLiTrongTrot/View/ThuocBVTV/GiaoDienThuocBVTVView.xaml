<UserControl x:Class="QuanLiTrongTrot.View.ThuocBVTV.GiaoDienThuocBVTVView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:QuanLiTrongTrot.View.ThuocBVTV"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1200">

    <Grid Background="#F3F6F7" Margin="14">
        <!-- ===== STYLE: tự chứa trong 1 file ===== -->
        <Grid.Resources>
            <SolidColorBrush x:Key="BrushText" Color="#0F172A"/>
            <SolidColorBrush x:Key="BrushSubText" Color="#64748B"/>
            <SolidColorBrush x:Key="BrushBorder" Color="#DCE3E7"/>
            <SolidColorBrush x:Key="BrushCard" Color="White"/>

            <!-- accent -->
            <SolidColorBrush x:Key="BrushGreen" Color="#2E7D32"/>
            <SolidColorBrush x:Key="BrushGreenSoft" Color="#E8F5E9"/>
            <SolidColorBrush x:Key="BrushRed" Color="#DC2626"/>
            <SolidColorBrush x:Key="BrushRedSoft" Color="#FEE2E2"/>
            <SolidColorBrush x:Key="BrushBlueSoft" Color="#E8F1FF"/>

            <Style x:Key="Card" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource BrushCard}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="14"/>
                <Setter Property="Padding" Value="14"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.12"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="Title" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            </Style>

            <Style x:Key="SectionTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            </Style>

            <Style x:Key="Label" TargetType="TextBlock">
                <Setter Property="Margin" Value="0,12,0,6"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#334155"/>
            </Style>

            <Style x:Key="Input" TargetType="TextBox">
                <Setter Property="Height" Value="36"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border CornerRadius="10"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource BrushGreen}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="Select" TargetType="ComboBox">
                <Setter Property="Height" Value="36"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="FontSize" Value="13"/>
            </Style>

            <Style x:Key="BtnBase" TargetType="Button">
                <Setter Property="Height" Value="38"/>
                <Setter Property="MinWidth" Value="92"/>
                <Setter Property="Padding" Value="14,8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <Style x:Key="BtnGreen" TargetType="Button" BasedOn="{StaticResource BtnBase}">
                <Setter Property="Background" Value="{StaticResource BrushGreen}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>

            <Style x:Key="BtnGhost" TargetType="Button" BasedOn="{StaticResource BtnBase}">
                <Setter Property="Background" Value="#F8FAFC"/>
                <Setter Property="Foreground" Value="#0F172A"/>
                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>

            <Style x:Key="BtnDanger" TargetType="Button" BasedOn="{StaticResource BtnBase}">
                <Setter Property="Background" Value="{StaticResource BrushRed}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>

            <!-- DataGrid style -->
            <Style TargetType="DataGrid">
                <Setter Property="RowHeaderWidth" Value="0"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="MinRowHeight" Value="36"/>
                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="HorizontalGridLinesBrush" Value="#EEF2F7"/>
            </Style>

            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#F1F5F9"/>
                <Setter Property="Foreground" Value="#0F172A"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Height" Value="38"/>
                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
            </Style>

            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="White"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#F8FAFC"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource BrushGreenSoft}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Style="{StaticResource Title}" Text="Danh mục Thuốc bảo vệ thực vật"/>
                <TextBlock Margin="0,4,0,0"
                           Foreground="{StaticResource BrushSubText}"
                           Text="Quản lý danh sách thuốc của cơ sở: tên thuốc, hoạt chất, nhóm, dạng, công dụng, quy cách/đóng gói."/>
            </StackPanel>

            <Border Grid.Column="1"
                    CornerRadius="999"
                    Padding="12,6"
                    Background="{StaticResource BrushBlueSoft}"
                    BorderBrush="#BBD7FF"
                    BorderThickness="1"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🏷" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Foreground="#1E3A8A" FontWeight="SemiBold"
                               Text="{Binding TenCoSo, FallbackValue='(Chọn cơ sở để xem thuốc)'}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- MAIN -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.1*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="1.4*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: TABLE -->
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Style="{StaticResource SectionTitle}" Text="Danh sách thuốc"/>

                    <!-- Search + Filter -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="230"/>
                        </Grid.ColumnDefinitions>

                        <Border CornerRadius="12" BorderBrush="#CBD5E1" BorderThickness="1" Background="White">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="{StaticResource BrushBlueSoft}" CornerRadius="12,0,0,12"/>
                                <TextBlock Grid.Column="0" Text="🔎" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         Height="36"
                                         Padding="0,0,10,0"
                                         VerticalContentAlignment="Center"
                                         Text="{Binding KeywordThuoc, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                                         ToolTip="Tìm theo tên thuốc / hoạt chất / nhóm / công dụng"/>
                            </Grid>
                        </Border>

                        <ComboBox Grid.Column="2"
                                  Style="{StaticResource Select}"
                                  SelectedValuePath="Tag"
                                  SelectedValue="{Binding FilterNhom, FallbackValue={x:Null}}"
                                  ToolTip="Lọc nhóm thuốc">
                            <ComboBoxItem Content="Tất cả nhóm" Tag="{x:Null}"/>
                            <ComboBoxItem Content="Trừ sâu" Tag="TruSau"/>
                            <ComboBoxItem Content="Trừ bệnh" Tag="TruBenh"/>
                            <ComboBoxItem Content="Trừ cỏ" Tag="TruCo"/>
                            <ComboBoxItem Content="Điều hòa sinh trưởng" Tag="DieuHoa"/>
                        </ComboBox>
                    </Grid>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="4"
                              ItemsSource="{Binding ThuocView, FallbackValue={x:Null}}"
                              SelectedItem="{Binding SelectedThuoc, Mode=TwoWay, FallbackValue={x:Null}}"
                              AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã" Binding="{Binding Id}" Width="90"/>
                            <DataGridTextColumn Header="Tên thuốc" Binding="{Binding TenThuoc}" Width="*"/>
                            <DataGridTextColumn Header="Hoạt chất" Binding="{Binding HoatChat}" Width="*"/>
                            <DataGridTextColumn Header="Nhóm" Binding="{Binding Nhom}" Width="160"/>
                            <DataGridTextColumn Header="Dạng" Binding="{Binding Dang}" Width="110"/>
                            <DataGridTextColumn Header="Công dụng" Binding="{Binding CongDung}" Width="220"/>
                            <DataGridTextColumn Header="Quy cách" Binding="{Binding QuyCach}" Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Note -->
                    <Border Grid.Row="6" CornerRadius="12" Padding="10"
                            Background="{StaticResource BrushRedSoft}" BorderBrush="#FCA5A5" BorderThickness="1">
                        <TextBlock Foreground="#991B1B" TextWrapping="Wrap"
                                   Text="Gợi ý: Nếu đề tài yêu cầu, bạn có thể thêm Số đăng ký, Hạn sử dụng, Nhà sản xuất…"/>
                    </Border>
                </Grid>
            </Border>

            <!-- RIGHT: FORM -->
            <Border Grid.Column="2" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="Thông tin thuốc"/>
                        <TextBlock Margin="0,4,0,0"
                                   Foreground="{StaticResource BrushSubText}"
                                   TextWrapping="Wrap"
                                   Text="Nhập/chỉnh sửa dữ liệu thuốc. Backend bạn nối vào nút Lưu/Xóa."/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Style="{StaticResource Label}" Text="Mã thuốc"/>
                            <TextBox Style="{StaticResource Input}"
                                     Text="{Binding Id, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                                     ToolTip="VD: T001"/>

                            <TextBlock Style="{StaticResource Label}" Text="Tên thuốc"/>
                            <TextBox Style="{StaticResource Input}"
                                     Text="{Binding TenThuoc, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                                     ToolTip="VD: Regent 800WG"/>

                            <TextBlock Style="{StaticResource Label}" Text="Hoạt chất"/>
                            <TextBox Style="{StaticResource Input}"
                                     Text="{Binding HoatChat, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                                     ToolTip="VD: Fipronil 800g/kg"/>

                            <TextBlock Style="{StaticResource Label}" Text="Nhóm thuốc"/>
                            <ComboBox Style="{StaticResource Select}"
                                      SelectedValuePath="Tag"
                                      SelectedValue="{Binding Nhom, UpdateSourceTrigger=PropertyChanged, FallbackValue=TruSau}">
                                <ComboBoxItem Content="Trừ sâu" Tag="TruSau"/>
                                <ComboBoxItem Content="Trừ bệnh" Tag="TruBenh"/>
                                <ComboBoxItem Content="Trừ cỏ" Tag="TruCo"/>
                                <ComboBoxItem Content="Điều hòa sinh trưởng" Tag="DieuHoa"/>
                            </ComboBox>

                            <TextBlock Style="{StaticResource Label}" Text="Dạng thuốc"/>
                            <ComboBox Style="{StaticResource Select}"
                                      SelectedValuePath="Tag"
                                      SelectedValue="{Binding Dang, UpdateSourceTrigger=PropertyChanged, FallbackValue=WG}">
                                <ComboBoxItem Content="EC" Tag="EC"/>
                                <ComboBoxItem Content="SC" Tag="SC"/>
                                <ComboBoxItem Content="WP" Tag="WP"/>
                                <ComboBoxItem Content="WG" Tag="WG"/>
                                <ComboBoxItem Content="GR" Tag="GR"/>
                            </ComboBox>

                            <TextBlock Style="{StaticResource Label}" Text="Công dụng"/>
                            <TextBox Style="{StaticResource Input}"
                                     Text="{Binding CongDung, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                                     ToolTip="VD: Trừ rầy nâu, sâu cuốn lá..."/>

                            <TextBlock Style="{StaticResource Label}" Text="Quy cách/Đóng gói"/>
                            <TextBox Style="{StaticResource Input}"
                                     Text="{Binding QuyCach, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                                     ToolTip="VD: Gói 10g, chai 100ml..."/>

                            <Border Margin="0,16,0,0"
                                    CornerRadius="14"
                                    Padding="12"
                                    Background="#F8FAFC"
                                    BorderBrush="#E2E8F0"
                                    BorderThickness="1">
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Foreground="{StaticResource BrushText}" Text="Gợi ý dữ liệu demo"/>
                                    <TextBlock Margin="0,4,0,0" Foreground="{StaticResource BrushSubText}" TextWrapping="Wrap"
                                               Text="• TenThuoc: Regent 800WG  • HoatChat: Fipronil  • Nhom: Trừ sâu  • Dang: WG  • QuyCach: 10g"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Buttons -->
                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource BtnGhost}" Content="Tạo mới" Command="{Binding NewThuocCommand}"/>
                        <Button Style="{StaticResource BtnGreen}" Content="Lưu" Command="{Binding SaveThuocCommand}"/>
                        <Button Style="{StaticResource BtnDanger}" Content="Xóa" Command="{Binding DeleteThuocCommand}"/>
                        <Button Style="{StaticResource BtnGhost}" Content="Bỏ chọn" Margin="0" Command="{Binding ClearSelectionCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
