<UserControl x:Class="QuanLiTrongTrot.View.ThuocBVTV.GiaoDienCoSoSanXuatThuocBVTV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:QuanLiTrongTrot.View.ThuocBVTV"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1200">

    <Grid Background="#F3F6F7" Margin="14">
        <!-- ===== STYLE: nằm hết trong 1 file ===== -->
        <Grid.Resources>
            <SolidColorBrush x:Key="BrushText" Color="#0F172A"/>
            <SolidColorBrush x:Key="BrushSubText" Color="#64748B"/>
            <SolidColorBrush x:Key="BrushBorder" Color="#DCE3E7"/>
            <SolidColorBrush x:Key="BrushCard" Color="White"/>

            <!-- theme -->
            <SolidColorBrush x:Key="BrushGreen" Color="#2E7D32"/>
            <SolidColorBrush x:Key="BrushGreenSoft" Color="#E8F5E9"/>

            <!-- BVTV accent -->
            <SolidColorBrush x:Key="BrushRed" Color="#DC2626"/>
            <SolidColorBrush x:Key="BrushRedSoft" Color="#FEE2E2"/>
            <SolidColorBrush x:Key="BrushAmber" Color="#F59E0B"/>
            <SolidColorBrush x:Key="BrushAmberSoft" Color="#FEF3C7"/>

            <Style x:Key="Card" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource BrushCard}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="14"/>
                <Setter Property="Padding" Value="14"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.12"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="Title" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            </Style>

            <Style x:Key="SectionTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            </Style>

            <Style x:Key="Label" TargetType="TextBlock">
                <Setter Property="Margin" Value="0,12,0,6"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#334155"/>
            </Style>

            <Style x:Key="Input" TargetType="TextBox">
                <Setter Property="Height" Value="36"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border CornerRadius="10"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource BrushGreen}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="Select" TargetType="ComboBox">
                <Setter Property="Height" Value="36"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="FontSize" Value="13"/>
            </Style>

            <Style x:Key="BtnBase" TargetType="Button">
                <Setter Property="Height" Value="38"/>
                <Setter Property="MinWidth" Value="92"/>
                <Setter Property="Padding" Value="14,8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <Style x:Key="BtnGreen" TargetType="Button" BasedOn="{StaticResource BtnBase}">
                <Setter Property="Background" Value="{StaticResource BrushGreen}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>

            <Style x:Key="BtnGhost" TargetType="Button" BasedOn="{StaticResource BtnBase}">
                <Setter Property="Background" Value="#F8FAFC"/>
                <Setter Property="Foreground" Value="#0F172A"/>
                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>

            <Style x:Key="BtnDanger" TargetType="Button" BasedOn="{StaticResource BtnBase}">
                <Setter Property="Background" Value="{StaticResource BrushRed}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>

            <!-- DataGrid style -->
            <Style TargetType="DataGrid">
                <Setter Property="RowHeaderWidth" Value="0"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="MinRowHeight" Value="36"/>
                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="HorizontalGridLinesBrush" Value="#EEF2F7"/>
            </Style>

            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#F1F5F9"/>
                <Setter Property="Foreground" Value="#0F172A"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Height" Value="38"/>
                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
            </Style>

            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="White"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#F8FAFC"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource BrushGreenSoft}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Style="{StaticResource Title}" Text="Quản lý cơ sở sản xuất Thuốc BVTV"/>
                <TextBlock Margin="0,4,0,0"
                           Foreground="{StaticResource BrushSubText}"
                           Text="Quản lý hành chính + tọa độ bản đồ (BandOld) + trạng thái giấy phép."/>
            </StackPanel>

            <Border Grid.Column="1"
                    CornerRadius="999"
                    Padding="12,6"
                    Background="{StaticResource BrushRedSoft}"
                    BorderBrush="#FCA5A5"
                    BorderThickness="1"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" Fill="{StaticResource BrushRed}" Margin="0,0,8,0"/>
                    <TextBlock Foreground="#991B1B" FontWeight="SemiBold" Text="Nhóm: Thuốc BVTV"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- MAIN -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="1.35*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: List -->
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Style="{StaticResource SectionTitle}" Text="Danh sách cơ sở"/>

                    <!-- Search / Filter -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="220"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Style="{StaticResource Input}"
                                 Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                                 ToolTip="Tìm theo mã / tên / địa chỉ / người đại diện / SĐT"/>

                        <ComboBox Grid.Column="2"
                                  Style="{StaticResource Select}"
                                  SelectedValuePath="Tag"
                                  SelectedValue="{Binding StatusFilter, FallbackValue={x:Null}}"
                                  ToolTip="Lọc trạng thái giấy phép">
                            <ComboBoxItem Content="Tất cả trạng thái" Tag="{x:Null}"/>
                            <ComboBoxItem Content="Đang hoạt động" Tag="DangHoatDong"/>
                            <ComboBoxItem Content="Đình chỉ" Tag="DinhChi"/>
                        </ComboBox>
                    </Grid>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="4"
                              ItemsSource="{Binding CoSoThuocView, FallbackValue={x:Null}}"
                              SelectedItem="{Binding SelectedCoSo, Mode=TwoWay, FallbackValue={x:Null}}"
                              AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã" Binding="{Binding Id}" Width="90"/>
                            <DataGridTextColumn Header="Tên cơ sở" Binding="{Binding TenCoSo}" Width="*"/>
                            <DataGridTextColumn Header="Địa chỉ" Binding="{Binding DiaChi}" Width="*"/>
                            <DataGridTextColumn Header="Người đại diện" Binding="{Binding NguoiDaiDien}" Width="160"/>
                            <DataGridTextColumn Header="SĐT" Binding="{Binding Sdt}" Width="130"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThaiGiayPhep}" Width="140"/>
                            <DataGridTextColumn Header="Lat" Binding="{Binding Lat}" Width="90"/>
                            <DataGridTextColumn Header="Lng" Binding="{Binding Lng}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Hint -->
                    <Border Grid.Row="6" CornerRadius="12" Padding="10"
                            Background="{StaticResource BrushAmberSoft}" BorderBrush="#FDE68A" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border CornerRadius="10" Background="{StaticResource BrushAmber}" Padding="8,4" VerticalAlignment="Center">
                                <TextBlock Foreground="White" FontWeight="Bold" Text="LƯU Ý"/>
                            </Border>
                            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"
                                       Foreground="#92400E"
                                       Text="Chọn cơ sở để xem/điền thông tin chi tiết. Danh mục thuốc sẽ đặt ở màn ThuocBVTVView."/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- RIGHT: Form -->
            <Border Grid.Column="2" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="Hồ sơ cơ sở"/>
                        <TextBlock Margin="0,4,0,0"
                                   Foreground="{StaticResource BrushSubText}"
                                   TextWrapping="Wrap"
                                   Text="Nhập/chỉnh sửa thông tin hành chính và định vị."/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Style="{StaticResource Label}" Text="Mã cơ sở"/>
                            <TextBox Style="{StaticResource Input}" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"/>

                            <TextBlock Style="{StaticResource Label}" Text="Tên cơ sở"/>
                            <TextBox Style="{StaticResource Input}" Text="{Binding TenCoSo, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"/>

                            <TextBlock Style="{StaticResource Label}" Text="Địa chỉ"/>
                            <TextBox Style="{StaticResource Input}" Text="{Binding DiaChi, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"/>

                            <TextBlock Style="{StaticResource Label}" Text="Người đại diện"/>
                            <TextBox Style="{StaticResource Input}" Text="{Binding NguoiDaiDien, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"/>

                            <TextBlock Style="{StaticResource Label}" Text="SĐT"/>
                            <TextBox Style="{StaticResource Input}" Text="{Binding Sdt, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"/>

                            <TextBlock Style="{StaticResource Label}" Text="Tọa độ (BandOld)"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Style="{StaticResource Input}" Text="{Binding LatText, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}" ToolTip="Latitude"/>
                                <TextBox Grid.Column="2" Style="{StaticResource Input}" Text="{Binding LngText, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}" ToolTip="Longitude"/>
                            </Grid>

                            <TextBlock Style="{StaticResource Label}" Text="Trạng thái giấy phép"/>
                            <ComboBox Style="{StaticResource Select}"
                                      SelectedValuePath="Tag"
                                      SelectedValue="{Binding TrangThaiGiayPhep, UpdateSourceTrigger=PropertyChanged, FallbackValue=DangHoatDong}">
                                <ComboBoxItem Content="Đang hoạt động" Tag="DangHoatDong"/>
                                <ComboBoxItem Content="Đình chỉ" Tag="DinhChi"/>
                            </ComboBox>

                            <Border Margin="0,16,0,0" CornerRadius="14" Padding="12"
                                    Background="{StaticResource BrushRedSoft}" BorderBrush="#FCA5A5" BorderThickness="1">
                                <TextBlock Foreground="#991B1B" TextWrapping="Wrap"
                                           Text="Gợi ý: Bạn có thể bổ sung Số giấy phép, Ngày cấp, Hạn sử dụng (nếu đề tài cần)."/>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Buttons -->
                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource BtnGhost}" Content="Tạo mới" Command="{Binding NewCommand}"/>
                        <Button Style="{StaticResource BtnGhost}" Content="Sửa" Command="{Binding EditCommand}"/>
                        <Button Style="{StaticResource BtnGreen}" Content="Lưu" Command="{Binding SaveCommand}"/>
                        <Button Style="{StaticResource BtnDanger}" Content="Xóa" Command="{Binding DeleteCommand}"/>
                        <Button Style="{StaticResource BtnGhost}" Content="Bỏ chọn" Margin="0" Command="{Binding ClearSelectionCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
